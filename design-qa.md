# TRZIP 디자인·데이터·동작 QA

- 검증일: 2026-08-15 KST
- 최신 디자인 기준: `Trend App Prototype Motion v2 (standalone).html`
- 금융 탐색 참고: 사용자가 제공한 미니스탁 종목 검색·종목 상세 화면 10장
- 구현 파일: `frontend/index.html`, `frontend/trendzip-data.js`, `frontend/mock-stock-universe.js`
- 검증 뷰포트: 393×852 모바일 프레임과 430×930 브라우저 뷰포트

## 판정

로컬 코드와 v4 데이터 계약은 `passed`입니다. 다만 운영 Vercel은 아직 v2 `live-data`를 읽고 있어 이번 변경을 production에 승격하지 않았습니다. 새 프런트는 v2 고정·합성 피드를 의도적으로 거부합니다. 다음 06:00 KST에 actual-only v4 publication이 원격 검증된 뒤 main을 승격하는 것이 배포 조건입니다.

## 디자인·기능 반영

- Motion v2의 요약 카드, 중립 신선도 카드, 단일 관심 흐름 카드, 폴더형 기업 역할, 하단시트 모션을 기존 TRZIP 디자인 토큰에 맞춰 이식했습니다.
- 디버그용 화면 전환 바를 운영 화면에서 제거했습니다.
- 스플래시·카드 선택·탭·폴더·하단시트에 짧은 전환 모션을 적용하고 `prefers-reduced-motion`을 지원합니다.
- 상태바 시간은 `Asia/Seoul` 기준 실제 시·분을 표시합니다.
- 긴 트렌드명은 홈·상세·목록에서 두 줄 제한과 줄바꿈으로 가로 넘침을 막습니다.
- 트렌드 아이콘은 같은 톤의 Twemoji SVG를 사용하며, 개기일식과 유성우를 서로 다른 실제 의미의 이미지로 구분합니다.
- 밈트폴리오는 원래 데모 4건과 사용자 이모지를 유지하고 라이브 트렌드·순위·시장자료와 분리했습니다.
- 종목 검색은 국내·해외 탭, 최근·인기 목록, 이름·영문명·종목코드 검색, 다중 선택·적용·해제를 지원합니다.
- 화면에 보이는 국내 15개·해외 12개 기업은 공식 출처의 검증된 SVG 또는 양축 64px 이상 로고를 사용합니다. 그 밖의 미검증 자산은 깨진 이미지 대신 이니셜로 표시합니다.

## 라이브 데이터 계약

- 홈은 최근 24시간 X·Google 실제 관측에서 완성 계약을 통과한 v4 카드만 0~10개 표시합니다.
- 고정 Top10, 이전 카드 재사용, 합성 추이, 기업 수 패딩을 사용하지 않습니다.
- 공개 카드 조건은 `main` 레인, 공개 맥락 URL, 키워드 정확히 5개, 근거 기업 정확히 10개, 역할 2~4개, 서로 다른 연결 키워드 2개 이상, 기업별 연결 근거입니다.
- NAVER 뉴스·공식 발표·기업 페이지는 맥락과 관계 근거만 보강하며 점수·순위를 바꾸지 않습니다.
- 4시간 체크포인트 승인 캐시는 원천 순위 상태를 변경하지 않고 완성도만 보강합니다.
- 실제 원장과 승인 캐시를 읽은 독립 빌드에서 v4 `ready`, `삼계탕` 1건, 키워드 5개, 기업 10개, 역할 4개, 합성·fallback 0건을 확인했습니다.

## 시장·기업 데이터

- 기업 하단시트는 `status=observed`, 기준일, 실제 통화, 공개 출처가 모두 확인된 시장자료만 표시합니다.
- 국내는 pykrx 가격과 Yahoo 재무 보강, 해외는 Yahoo 가격·재무·환율 경로를 사용합니다.
- 시가총액은 모든 시장에서 원화로만 표시하며 원통화 시가총액·환율·기준일·출처를 감사 필드로 보존합니다.
- ROE는 TTM 우선·연간 fallback과 두 시점 평균 자기자본을 사용합니다. 분기 순이익이나 자기자본 이력이 부족하면 값을 만들지 않습니다.
- 실제 호출에서 삼성전자와 Apple 모두 30일 관측, 원화 시가총액, 공개 출처와 ROE 산식 근거를 확인했습니다.
- 검증 자료가 없으면 숫자를 생성하지 않고 `시장 자료를 확인하지 못했습니다` 상태를 표시합니다.
- 고객 화면에서 관계 등급·근거 상태를 숨기고, 기업 설명과 `트렌드와 연결된 이유`만 제공합니다.
- 하단 CTA는 왼쪽 `관심기업 등록`, 오른쪽 `키움 종목홈으로 가기`입니다.

## 브라우저 E2E

- 스플래시 → 홈 전환 및 KST 실시간 시계
- actual-only 데이터가 없을 때 정직한 홈 빈 상태
- 기존 데모 밈트폴리오의 사용자 이모지·목적 유지
- 밈트폴리오 만들기 → 종목 검색 → 국내·해외 탭 → 2개 종목 선택 → 적용 → 삭제
- 종목 검색 목록이 화면 헤더·탭·하단 버튼을 침범하던 중첩 `section` CSS 결함 수정
- JSON·CSV 내보내기와 디버그 화면 전환 바 미노출
- 깨진 이미지·가로 오버플로·브라우저 콘솔 오류 없음

## 자동 검증

- 전체 테스트: `489 passed, 1 skipped`
- Python compileall: passed
- PowerShell 스크립트 구문 검사: passed
- JavaScript 파일 구문 검사: passed
- 공개 v4 schema·publication validator·result quality: passed
- Git diff 공백 검사: passed
- 실제 시장 API smoke: 삼성전자·Apple `observed`

## 운영상 남은 게이트

- 마지막 실제 시간별 수집은 2026-08-15 14:00 KST이며, 장시간 개발 작업 동안 이후 정각 실행이 기동되지 않았습니다. 다음 자동 실행부터 공백을 그대로 기록하고 재사용·보간하지 않습니다.
- 원격 `live-data`는 아직 v2입니다. 다음 06:00 KST v4 publication과 원격 SHA 검증 전에는 새 프런트를 production main에 올리지 않습니다.
- LLM 실행 연결값은 아직 없어 자동 의미 판별은 비활성입니다. 현재 4시간 handoff는 NAVER 뉴스·공식 공개 근거와 검증된 reviewed cache로 보강합니다.
- 키움 S# 종목 딥링크·계정 관심종목 동기화와 analytics 수집 서버는 외부 연동 범위로 남아 있습니다.
